// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: local_profile.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createLocalProfile = `-- name: CreateLocalProfile :one
INSERT INTO local_profile (
    id,
    name,
    gender,
    email,
    status,
    role_id
) VALUES (
    $1, $2, $3, $4, $5, $6
)
RETURNING id, name, gender, email, status, role_id
`

type CreateLocalProfileParams struct {
	ID     uuid.UUID   `json:"id"`
	Name   string      `json:"name"`
	Gender interface{} `json:"gender"`
	Email  pgtype.Text `json:"email"`
	Status interface{} `json:"status"`
	RoleID pgtype.UUID `json:"role_id"`
}

func (q *Queries) CreateLocalProfile(ctx context.Context, arg CreateLocalProfileParams) (LocalProfile, error) {
	row := q.db.QueryRow(ctx, createLocalProfile,
		arg.ID,
		arg.Name,
		arg.Gender,
		arg.Email,
		arg.Status,
		arg.RoleID,
	)
	var i LocalProfile
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Gender,
		&i.Email,
		&i.Status,
		&i.RoleID,
	)
	return i, err
}

const getLocalProfileByID = `-- name: GetLocalProfileByID :one
SELECT id, name, gender, email, status, role_id
FROM local_profile
WHERE id = $1
`

func (q *Queries) GetLocalProfileByID(ctx context.Context, id uuid.UUID) (LocalProfile, error) {
	row := q.db.QueryRow(ctx, getLocalProfileByID, id)
	var i LocalProfile
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Gender,
		&i.Email,
		&i.Status,
		&i.RoleID,
	)
	return i, err
}

const listLocalProfiles = `-- name: ListLocalProfiles :many
SELECT id, name, gender, email, status, role_id
FROM local_profile
ORDER BY id
`

func (q *Queries) ListLocalProfiles(ctx context.Context) ([]LocalProfile, error) {
	rows, err := q.db.Query(ctx, listLocalProfiles)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []LocalProfile
	for rows.Next() {
		var i LocalProfile
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Gender,
			&i.Email,
			&i.Status,
			&i.RoleID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
